node_modules
dist
.next
.turbo
.env
.env.local
.neon
.DS_Store

# TypeScript incremental build cache; rewritten on every compile
*.tsbuildinfo

# Regenerated by next dev and next build, which point it at different .next paths
next-env.d.ts

# Legacy formforge SQL kept locally for reference only
db/formforge/
.vercel
.env*

# Local Claude Code instructions
CLAUDE.md

# Per-developer editor settings
.vscode/settings.json
